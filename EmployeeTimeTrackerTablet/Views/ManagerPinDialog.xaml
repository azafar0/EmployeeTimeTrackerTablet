<Window x:Class="EmployeeTimeTrackerTablet.Views.ManagerPinDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manager Authentication Required"
        Height="400"
        Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="#F8F9FA"
        FontFamily="Segoe UI"
        FontSize="14">

    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0056B3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004085"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Cancel Button Style -->
        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="#6C757D"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#545B62"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#3D4247"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- PIN TextBox Style -->
        <Style x:Key="PinTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#007BFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- PIN PasswordBox Style -->
        <Style x:Key="PinPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#007BFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="30"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#40000000" BlurRadius="10" ShadowDepth="3" Direction="315"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Main Content Card -->
        <Border Style="{StaticResource CardStyle}" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center">
            <StackPanel>
                <!-- Header -->
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center" 
                            Margin="0,0,0,20">
                    <TextBlock Text="&#xE72E;" 
                               FontFamily="Segoe UI Symbol" 
                               FontSize="32" 
                               Foreground="#FFC107" 
                               Margin="0,0,15,0" 
                               VerticalAlignment="Center"/>
                    <TextBlock Text="Manager Authentication" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               Foreground="#495057" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Description -->
                <TextBlock Text="Time correction requires manager approval." 
                           FontSize="14" 
                           Foreground="#6C757D" 
                           TextAlignment="Center" 
                           Margin="0,0,0,10"/>
                
                <TextBlock Text="Please enter the 4-digit manager PIN:" 
                           FontSize="16" 
                           Foreground="#495057" 
                           TextAlignment="Center" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,25"/>

                <!-- PIN Entry -->
                <PasswordBox x:Name="PinPasswordBox" 
                             Style="{StaticResource PinPasswordBoxStyle}"
                             MaxLength="4" 
                             Width="150" 
                             Height="50"
                             Margin="0,0,0,10"
                             KeyDown="PinPasswordBox_KeyDown"
                             PasswordChanged="PinPasswordBox_PasswordChanged"/>

                <!-- PIN Hint -->
                <TextBlock Text="Enter 4 digits" 
                           FontSize="12" 
                           Foreground="#6C757D" 
                           TextAlignment="Center" 
                           Margin="0,0,0,20"/>

                <!-- Error Message -->
                <TextBlock x:Name="ErrorMessage" 
                           Text="" 
                           FontSize="14" 
                           Foreground="#DC3545" 
                           TextAlignment="Center" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,15" 
                           Visibility="Collapsed"/>

                <!-- Status Message -->
                <TextBlock x:Name="StatusMessage" 
                           Text="" 
                           FontSize="14" 
                           Foreground="#28A745" 
                           TextAlignment="Center" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,15" 
                           Visibility="Collapsed"/>

                <!-- Loading Indicator -->
                <StackPanel x:Name="LoadingPanel" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Center" 
                            Margin="0,0,0,15" 
                            Visibility="Collapsed">
                    <TextBlock Text="&#xE1CD;" 
                               FontFamily="Segoe UI Symbol" 
                               FontSize="16" 
                               Foreground="#007BFF" 
                               Margin="0,0,10,0" 
                               RenderTransformOrigin="0.5,0.5">
                        <TextBlock.RenderTransform>
                            <RotateTransform x:Name="LoadingRotation"/>
                        </TextBlock.RenderTransform>
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="LoadingRotation" 
                                                         Storyboard.TargetProperty="Angle" 
                                                         From="0" To="360" 
                                                         Duration="0:0:2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                    <TextBlock Text="Authenticating..." 
                               FontSize="14" 
                               Foreground="#007BFF" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Button Panel -->
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center" 
                            Margin="0,20,0,0">
                    <Button x:Name="AuthenticateButton" 
                            Content="Authenticate" 
                            Style="{StaticResource ModernButtonStyle}" 
                            Width="120" 
                            Click="AuthenticateButton_Click"
                            IsEnabled="False"/>
                    <Button x:Name="CancelButton" 
                            Content="Cancel" 
                            Style="{StaticResource CancelButtonStyle}" 
                            Width="120" 
                            Click="CancelButton_Click"/>
                </StackPanel>

                <!-- Additional Info -->
                <TextBlock Text="Manager authentication is required for time corrections exceeding 16 hours." 
                           FontSize="11" 
                           Foreground="#6C757D" 
                           TextAlignment="Center" 
                           Margin="0,20,0,0" 
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>